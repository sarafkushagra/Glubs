<!DOCTYPE html>
<html lang="en">

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Events | Glubs</title>
    <link rel="stylesheet" href="../css/allevent.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;300;400;500;600;700;800&display=swap"
        rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/output.css" rel="stylesheet">
</head>

<body>
    <section class="header_events">
        <nav>
            <a href="/"><img src="../images/Preview.png"></a>
            <div class="nav-links" id="nav-links">
                <ul>
                    <li><a href="/">HOME</a></li>
                    <li><a href="/about">ABOUT</a></li>
                    <li><a href="/events">EVENTS</a></li>
                    <li id="login"><a href="/users/login">LOGIN</a></li>
                </ul>
            </div>
        </nav>
    </section>
    
    <section class="text-content">

        <div class="text-box">
            <h1>All Events</h1>
            <p>Browse all upcoming and past events</p>
        </div>
    </section>
    <section class="events-list container mt-5">
        <% if (events && events.length> 0) { %>
            <div class="row">
                <% events.forEach(function(event) { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="card-title">
                                    <%= event.title %>
                                </h5>
                                <p class="card-text">
                                    <%= event.description %>
                                </p>
                                <p class="card-text"><small class="text-muted">Date: <%= event.date %></small></p>
                                <a href="/event/<%= event._id %>" class="btn btn-primary">View Details</a>
                            </div>
                        </div>
                    </div>
                    <% }); %>
            </div>
            <% } else { %>
                <p>No events found.</p>
                <% } %>
    </section>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>